{
  "name": "pro-climbing-viewer",
  "description": "Placeholder package for dependencies -- see manifest.json for project version and description",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "web-ext lint",
    "start:firefox": "web-ext run",
    "build": "web-ext build --overwrite-dest",
    "sign": "web-ext sign --channel unlisted",
    "publish": "web-ext sign --channel listed"
  },
  "webExt": {
    "ignoreFiles": [
      "package.json",
      "package-lock.json",
      "README.md"
    ]
  },
  "devDependencies": {
    "web-ext": "^8.0.0"
  }
}
